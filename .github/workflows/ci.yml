name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  backend:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: python -m pip install -r requirements-dev.txt
      - run: ruff check backend/app agent_eval
      - run: coverage run -m pytest -q
      - run: coverage report --fail-under=75
      - if: matrix.python-version == '3.11'
        run: python agent_eval/run_batch_eval.py
      - name: Run enterprise evaluation when private fixture is available
        if: matrix.python-version == '3.11'
        run: |
          if [ -f "external_eval_data/enterprise_agent_eval_dataset_20260703_221458/benchmarks.json" ] || [ -f "external_eval_data/enterprise_agent_eval_dataset_20260703_221458.zip" ]; then
            python agent_eval/enterprise_business_eval.py
          else
            echo "::notice::Enterprise evaluation skipped: its external fixture is not part of the public repository. Run it locally with --data-dir or provide the fixture through a protected workflow."
          fi

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm test
      - run: npm run build
      - run: npm audit --audit-level=high

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: python -m pip install -r requirements.txt
      - working-directory: frontend
        run: npm ci
      - working-directory: frontend
        run: npx playwright install --with-deps chromium
      - working-directory: frontend
        run: npm run test:e2e

  containers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker compose config --quiet
      - run: docker compose build
